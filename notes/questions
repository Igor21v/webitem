Почему не срабатывает useEffect когда убираем dispatch из массива зависимостей в App.tsx? Урок 48
В public api Article указали export type {Article}, зачем прописывать type? Урок 49
При загрузке комментариев не отображаются скелетоны, так как массив коментов пуст.
При загрузке страницы с лоадером окно страницы уменьшается

По хуку useThrottle большой вопрос! При диспатче в хуке обновляется стейт и тянет за собой обновление компонента вызывающего хук (Page.tsx)
при этом колбек который использует хук пересоздается и useCallback внутри хука не действует.
С помощью девтулза реакт видно что вся страница рендерится заново.
Вероятно нужно вместо хука сделать простой хелпер, и вызывать его через useCallback
 (пользовательский хук нельзя вызвать из другого хука) Урок 58

В селекторе ui.ts getUIScrollByPath не до конца понятен, передаем функцию с Page.tsx но как это работает вопрос не до конца ясен

Доделать:
тесты на сервис fetchArticleById
подгрузка доп статей когда на экран при первой загрузки входят все
При типизации селекта убрал мемоизацию так как для использования с типизацией потребуется дополнительно что то городить
Вход в форме авторизации с Enter
Сдалать расположение элемонтов при плитке Masonry, пример есть в react virtualized
В компоненте ArticleList.tsx захардкожено количество элементов в строке, нужно сделать вычисление их
В компоненте текст используем заголовки для изменения размера текста, (66 урок). Пишут что так делать не стоит, семантикой
не нужно добиваться изменения стилей. Заголовок H1 должен быть один на странице, а крупного текста может быть много. Нужно
переделать так чтобы семантика определялась в тексте отдельным тэгом
Демонтировать библиотеку Headless (25 kB в бандле)
Не анализируется положение выпадающих дропдаунов (может выпасть за пределы страницы, нужно смотреть положение и делать
список выпадающий вверх при необходимости, рекомендована либа floating ui, весит 7,5 кВ)
Бесит что задисейбленная кнопка при наведении подсвечивается.
В ArticleList заигнорены ошибки typescript, связно с устаревшей библиотекой реакт виртуалайзд, нужно заменить на библиотеку react-virtuoso.
Некоторые компоненты в сторибуке не работают, необходимо обернуть в провайдеры, добавить состояния и т.д. (Не делаю сразу т.к. в конечном итоге проек будет 
кординально переработан под другие задачи, и эти компоненты возможно удаляться)
Не панель нотификаций монтируется в дом при рендеренге странице и не при закрытии дровера не демонтируется
Не работает анимация появления модального окна.
При переполнение уведомлений в мобилке не появляется скролл
При открытии Dropdown выпадает в консоль предупреждение Warning: Function components cannot be given refs.
В Drawer lazy никуда не привязан
При отправке запроса мутации данных с выставлением оценки, если нажать назад а потом опять на статью то оценки не будет, так как данные по оценкам уже запрошены, 
RTK берет их и не формирует новый запрос.
